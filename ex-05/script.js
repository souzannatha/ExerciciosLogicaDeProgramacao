/*Escreva uma função que receba uma string como parâmetro e retorne a mesma string, 
mas com todas as letras em maiúsculas.*/

let palavra= "ola";
function transformarMaiusculo(palavra) {
  return palavra.toUpperCase();
}

console.log(transformarMaiusculo(palavra));
